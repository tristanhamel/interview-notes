export function scoring(questionnaires, questions) {
  // assign a score to each response if it's set to auto
  const responses = questionnaires.map(q => Object.assign({}, q.responses, ));
    questions
    .forEach(question => {
    });
}
