import * as actions from '../constants/ActionTypes';

const initialState = [];

export const questionnaires = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
